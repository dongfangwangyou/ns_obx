name: ns_obx
description: "A lightweight, high-performance Flutter reactive state management library. A fine-grained reactive solution extracted from GetX's reactive core, supporting Obx, reactive variables, and collection types."
version: 1.0.6
topics:
  - state-management
  - reactive
  - getx
  - obx
  - rx
homepage: https://github.com/dongfangwangyou/ns_obx
repository: https://github.com/dongfangwangyou/ns_obx
issue_tracker: https://github.com/dongfangwangyou/ns_obx/issues

environment:
  sdk: '>=3.4.0 <4.0.0'
  flutter: ">=3.13.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
